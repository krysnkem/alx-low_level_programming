#ifndef FUNCTION_LIKE_MARCO_H
#define FUNCTION_LIKE_MARCO_H
/**
 * ABS(x) - computes the absolute value of a number
 * @x: the input value
 *
 * Return the absolute value of x
 */

#define ABS(x) ((x) < (0) ? ((x) * -1) : (x))
#endif /*FUNCTION_LIKE_MARCO_H*/
